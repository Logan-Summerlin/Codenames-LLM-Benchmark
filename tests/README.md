# Tests

This directory is reserved for deterministic unit tests for the Codenames LLM benchmark.

The first tests should cover game-state transitions, board generation reproducibility, clue legality, role-specific observations, and event-log replay.
