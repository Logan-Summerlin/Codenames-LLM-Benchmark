# Report Schema

Reports are Markdown documents with two required sections: `Ratings` and `Diagnostics`.
Ratings are model-name to Elo-like score mappings. Diagnostics are aggregate event-log metrics such as total guesses, clue count, opponent hits, neutral hits, assassin hits, illegal clues, and clue efficiency.
